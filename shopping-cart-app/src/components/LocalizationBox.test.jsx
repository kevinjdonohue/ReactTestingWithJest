import LocalizationBox from './LocalizationBox';

describe("LocalizationBox", () => {
    describe("getInitialState", () => {
        it("should return initial state values", () => {
            /*
            //arrange
            let localizationBox = new LocalizationBox();

            //act
            let state = localizationBox.getInitialState();

            //assert
            expect(state.country).toBe("foo");
            */
        });
    })
});